version: 0.2

phases:
  install: 
    runtime-versions:
      nodejs: 14.17.0
  build:
    commands:this is a build phase... Zipping my webpage"
      -echo ""
      - echo "Package & Dependicies are getting installed"
      - npm -f install
      - echo "Packages have been installed"
      - echon "Storing Build Artifact to the Location"
      post_build:
      -echo "Deplpoyed to s3"
artifacts:
  type: zip
  files: 
  - '**/*'
  paths:
    - '*'